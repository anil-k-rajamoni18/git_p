git clone <url>

git init <dir_name>

git init 

git add <file_name>

git add . :-> all files

git add -a :-> all files

git commit -m "commit message"

git commit -a :->

git status :-> let know about the changes in different phases.


git push origin master

git push --all :-> push all the branches

git pull <url> 

git remote add <url>

git stash show

git stash show -p

git stash apply <stash_id>

git stash list

git stash pop : it will delete the stash_id after re-applying

git stash drop

git stash clear

git stash branch <Branch_Name>


